# Security policy

Do not report credentials through a public issue. Contact the corresponding
author privately if a credential or sensitive configuration is found.

D-Wave API tokens must be stored only in the user's Ocean configuration or a
machine-local environment. They must never appear in source code, JSON configs,
logs, result archives, screenshots, or Git history.
